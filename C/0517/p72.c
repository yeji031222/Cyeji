//pow()와 sqrt
#include <stdio.h>
#include <math.h> // 수학 함수들을 포함하는 표준 라이브러리

int main()
{
printf("10의 3승은 %.0f.\n", pow(10, 3)); //pow() 함수는 거듭제곱을 계산
printf("16의 제곱근은 %.0f.\n", sqrt(16)); //sprt() 함수는 제곱근을 계산
}